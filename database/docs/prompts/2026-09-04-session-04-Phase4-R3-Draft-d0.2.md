# Phase 4 R3 — 초안 `d0.2` 작성 후 중단

작업 디렉터리: `D:\AIDEV\HealthCheckupReservationReception\database`
작업 branch: **`phase4-r3`** (`c7a0a61`). 이미 체크아웃돼 있다
선행 세션: `database-63` — 초안 `d0.1` 작성 + 적대적 검토 5인 + 기존 결함 3건 수정
복원점: branch `phase4-database`(`fdfb8e6`) · tag `phase4-r2-t07-complete`

---

## 1. 목표

**초안 `d0.1` 을 `d0.2` 로 고쳐 쓰고 멈춘다.**

`00`·`01`·`02`·`03`·`04`·`05`·`06`·계획 9개·게이트·실물 재배포로 **넘어가지 않는다.** 초안이 흔들린 채 하위를 고치면 전부 두 번 쓰게 된다. 이것은 선행 세션이 이미 한 번 겪은 실패다 — `d0.1` 은 하위 영향 목록을 먼저 쓰다가 상위 정책 위반 5건을 놓쳤다.

`d0.2` 의 일은 셋이다.

```text
① 검토 결과 48건을 초안에 반영한다
② 계층 A 5건(최상위 정책 위반)의 개정안을 설계한다 — 개봉은 승인됐고 설계가 없다
③ 그 결과를 다시 압박한다
```

## 2. 지금 사실인 것

| 항목 | 상태 |
|---|---|
| branch | **`phase4-r3`** `c7a0a61`. `phase4-database` `fdfb8e6` 에서 분기. remote 없음 |
| tag | `baseline-HC-RSV-RCP-20260903-R2` · `phase4-r2-t07-complete` |
| 기준선 문서 | `docs/baseline/00`~`05` — **R2 원본 그대로. 아직 한 바이트도 안 바뀌었다** |
| 스펙 | `docs/phase4/06_…_CANDIDATE.md` v0.3 — R2 기준 |
| 계획 | `docs/phase4/plans/` 9개 — R2 기준. 단 컬럼 오참조 7곳은 `fdfb8e6` 에서 수정됨 |
| 초안 | `docs/phase4/04_DB_Design_R3_DRAFT.md` **d0.1 — 검토를 통과하지 못했다** |
| 검토서 | `docs/phase4/04_DB_Design_R3_DRAFT_REVIEW.md` — 결함 48건. **이 세션의 입력이다** |
| 배포된 DB | `.\SQLEXPRESS` 의 `HealthCheckupReservationReceptionDb` 에 **R2 스키마 7테이블**. 건드리지 않는다 |
| 게이트 | `node tools/verify-docs.js` PASS 15 / FAIL 0 · `./scripts/verify-baseline.sh` 6/6 · `./scripts/verify-winforms-unchanged.sh` 0건. **세 개 모두 통과 상태** |
| `jszip` | **전역 설치됨.** `NODE_PATH="$(npm root -g)"` 로 `require('jszip')` 해결 |

**먼저 읽을 것** (순서대로 — ②③ 을 건너뛰지 마라)

1. `CLAUDE.md` — 경계·금지사항·실행 규칙
2. **`docs/phase4/04_DB_Design_R3_DRAFT_REVIEW.md` 전문** — 결함 48건. **재검토하지 마라. 이미 원문 대조로 확인됐다**
3. `docs/phase4/04_DB_Design_R3_DRAFT.md` — 고칠 대상 (1141줄)
4. `docs/prompts/2026-09-04-session-03-Phase4-R3-Redesign.md` — R3 개편의 원래 인계문서. **§3.3 의 "`01`·`02` 를 열지 않는다" 는 거짓으로 판명됐다**
5. `docs/baseline/00_Project_Policy.md` — 이번에 여는 최상위 정책
6. `docs/baseline/04_DB_Design.md` — 개편 대상 원본 (1649줄)

## 3. 승인된 결정 — 재논의 대상이 아니다

| 결정 | 승인 |
|---|---|
| **`00`·`01`·`02` 를 열고 R3 전체를 진행한다** | 2026-09-04 사용자 확정. 대안 2종(R3 보류 후 T08 재개 / `00` 을 안 여는 범위로 축소)을 함께 제시한 뒤 선택 |
| Write SP 8개에 `@OperatorName NVARCHAR(50)` 추가 (`05` §19.2 Parameter 항목 개봉) | 2026-09-04 사용자 확정 |
| 기준선 ID `HC-RSV-RCP-20260904-R3` · 기준일 `2026-09-04`(설계 확정일 고정) | 2026-09-04 사용자 확정 |
| 기존 결함 3건(E1·C4·C5) 선행 수정 | 2026-09-04 사용자 확정. `fdfb8e6` 에서 완료 |

`d0.1` 의 §2(테이블 6개·수검자 18컬럼·상태 5값·`변경이력`·명명)는 선행 세션 `database-38` 의 grilling 6라운드 결과다. **다만 검토에서 그중 셋이 `00` 을 여는 것으로 밝혀졌으므로, 유지하되 상위 개정안을 함께 설계해야 한다.**

## 4. 반드시 해소해야 할 것 — 계층 A (검토서 §1)

개봉 승인은 받았다. **개정문이 없다.** 이것이 `d0.2` 의 가장 큰 일이다.

| ID | 무엇을 써야 하는가 |
|---|---|
| **A1** | `00`:49 **CP-06** 개정문. 현재 *"일반 변경의 상세 이력은 별도 History로 관리하지 않는다"*. 전면 허용인가, *"업무 단위 1행에 한한다"* 인가. `02` `S6:5` 가 `CP-06` 을 추적하므로 `02` 까지 파급된다 |
| **A2** | `00` §6 상태 해석(전이도 + Action Matrix 3행) · :33 용어표 · CP-05 · RP-03 · RP-10 · RCP-06 · TGT-02, `01` §7 전이 4줄 · 최종확정 #9 재작성. **`CNL` → `CNR`/`CNC` 분리만으로도 열린다 — `FIN` 때문이 아니다** |
| **A4** | **TGT.** `HIS_GENERAL_CHECKUP_COMPLETIONS` 를 존치(테이블 7개 유지)할 것인가, `00`:196 TGT-04 를 개정해 판정 변화를 계약으로 수용할 것인가. 반례는 검토서 §1 A4 에 실증돼 있다 — **컬럼 하나로는 `CompletionDate < ReservationDate` 필터 뒤의 `MAX()` 를 표현할 수 없다.** 이것은 취향이 아니라 구조다 |
| **A5** | `FIN` 을 `00` RP-03(정원) · RP-06(중복) · TGT-02 각각에서 어느 쪽에 넣는가. 넣지 않으면 슬롯 20건을 `FIN` 처리해 **하루 40명**이 가능하다. 그리고 `FIN` 도달 경로(SSMS 직접 `UPDATE`)가 `04` §1.3 · `06` §32 를 우회하는 문제 |
| **A3** | `00`:70 EP-02 · :87 · `01`:189 · :426 의 물리 식별자를 한글명으로 바꿀 것인가, 대응표를 둘 것인가 |

## 5. 반드시 재설계할 것 — 초안이 틀린 것

| ID | 재설계 |
|---|---|
| **C1·C2** | **`V08` 계수 방식.** `d0.1` §12.4 의 정규식 확장안은 **틀렸다** — 한국어 조사를 이름에 흡수하고 R2 이름에서 기존 정확동작을 회귀시킨다(실측). 그리고 `d0.1` §3.2·§3.3·§12.4 의 예시가 스스로 오염원이라 실측이 **CK=26 DF=9** 가 된다(선언 22/8). **`04` 의 테이블 상세 정의 절만 잘라내고 그 안의 백틱 한정 이름만 세는 방식**으로 바꾼다 — `` /`(?:CK\|DF)_[^`]+`/g ``. 백틱이 경계라 조사 흡수가 원리적으로 불가능하고 §3 예시가 절 밖이라 자동 제외된다 |
| **B1** | `변경이력` 기록 지점. `d0.1` §4.3 의 3곳이 `06` §21.1 제어흐름에서 **7가지 방식으로 깨진다**(검토서 §2.1). 단일 수정안: **기록은 항상 트랜잭션 밖에서, 자체 `TRY/CATCH`(빈 `CATCH`)로 오류를 삼키며, 해당 Result Set `SELECT` 뒤에** 한다. 대가로 *"성공 기록의 원자성"* 을 포기하고 §10 한계로 등재한다 |
| **B2** | 업무 `INSERT` 직후 키를 변수로 확정한 뒤 감사 `INSERT`. 안 그러면 `SCOPE_IDENTITY()` 가 `PatientId`/`WorkId` 가 아니라 `HistoryId` 를 돌려준다 |
| **B3** | 명명규칙. `d0.1` §3.1 이 §6 이름 **11개를 만들지 못하고** §3.2 의 *"테이블 구간만 치환"* 은 FK 3개 전부에서 거짓이다. *"본체는 R2 본체를 승계한다. 신규 객체에만 컬럼명 `SNAKE_CASE` 전개"* 로 바꾸고 FK 를 명시적 예외로 둔다 |
| **D16** | `Rebuild.sql` `50022` 를 전환 1회에 한해 *"정확한 R2 집합 **또는** 정확한 R3 집합"* 이중 가드로 둔다. `d0.1` 은 한 방향만 적어 **마이그레이션 Rebuild 자체가 막히는 순서**를 유도한다 |
| **D4** | `Parameter 87 → 95`. `06` §36·G09·`:2370` + `plans/08` `@ExpParam`. **`EXCEPT` 양방향**이라 `@OperatorName` 8행을 **정확한 ordinal 위치**에 넣어야 한다 |

나머지 B·C·D 항목은 검토서 §2~§4 의 표를 그대로 작업목록으로 쓴다. **§12 영향 목록은 20건이 빠져 있어 지금 상태로는 작업목록으로 쓸 수 없다** — 특히 `plans/**` 행이 하나도 없다.

## 6. 절대 금지

```text
docs/baseline/** 수정                     ← d0.2 는 초안이다. 실제 교체는 4단계 이후다
docs/phase4/06_*.md 와 plans/** 수정       ← 이 세션 범위 밖
database/deploy/** · database/tests/** 수정 ← 범위 밖
../winforms/** 수정
phase4-database branch 로 되돌아가 커밋하는 것
ROOT tools/ 와 docs/baseline/output/ 을 커밋하는 것   ← 다른 세션(문서 생성)의 산출물이다
git config --global 변경 / remote 추가 / push
git reset · clean · checkout -- · force push · 기존 tag 이동
Net461MvpSample DB 접속·변경·삭제         ← 같은 인스턴스에 있다
실제 주민등록번호 사용
검토서 48건을 처음부터 다시 검증하는 것    ← 이미 원문 대조로 확인됐다. 시간을 쓰지 마라
```

## 7. 실측으로 확인된 함정 — `CLAUDE.md` 에 없는 것

`CLAUDE.md` 에 BOM · `-b -I -u` · `N` 접두사 · `set -e` 회피 · `iconv -f UTF-16` 이 있다. **먼저 읽는다.** 아래는 거기 없다.

| 함정 | 실측 결과 |
|---|---|
| **`V11` 이 `04` 와 `plans` 를 묶는다** | `fdfb8e6` 이 만든 새 결합이다. `04` 를 한글 테이블명으로 바꾸는 순간 계획의 R2 이름 **578곳이 전부 FAIL** 한다. 의도한 것이지만 최소 원자 단위가 커졌다 |
| `02_Function_Definition.xlsx` 파싱 | `x:` **네임스페이스 접두사**를 쓰고 `sharedStrings` 가 **비어 있다**(전부 `inlineStr`). 접두사를 무시하는 파서는 **시트 0개·문자열 0개로 조용히 통과**한다 — 정규식에 `(?:[A-Za-z0-9]+:)?` 를 넣어야 한다 |
| `02` 에는 생성기가 없다 | `tools/docgen/xlsx/build_02.js` 는 기준선 `02` 를 **읽어서** 공개용 xlsx 를 만드는 **배포기**다. 기준선 `02` 자체를 만드는 생성기는 저장소에 없다 |
| `PRINT` 인자에 하위 쿼리 | **`Msg 1046`** 으로 **배치 전체가 컴파일 실패**한다. 앞 문장이 한 줄도 실행되지 않는데 exit 1 이라 시험이 통과한 것처럼 보인다. 변수에 먼저 담는다 |
| `master` 컨텍스트에서 `sys.databases.name + '문자열'` | **`Msg 451`** — catalog collation 충돌. sqlcmd `-s"\|"` 로 회피. **대상 DB 안에서는 일어나지 않는다** |
| `sys.databases.collation_name` | Express 가 `model` 로부터 **`AUTO_CLOSE ON`** 을 상속해 DB 가 닫혀 있으면 `NULL` 을 돌려준다. diff 기준으로 쓰면 안 된다 |
| 필터형 인덱스 | 생성 시점뿐 아니라 **그 테이블의 모든 `INSERT`/`UPDATE`/`DELETE` 시점에도** `QUOTED_IDENTIFIER ON` 을 요구한다. 없으면 `Msg 1934` |
| `CHAR(3)` 에 `'DONE'` | 4글자라 안 들어간다. 상태값을 `FIN` 으로 정한 이유다 |
| SQL Server Agent | 이 Express 인스턴스에서 **Stopped** |

## 8. 완료 판정 — 전부 참이어야 이 세션이 끝난다

```text
1. docs/phase4/04_DB_Design_R3_DRAFT.md 가 d0.2 이고 검토서 48건이 전건 반영됐다
2. 계층 A 5건 각각에 00·01·02 개정안이 초안 안에 문장으로 있다
3. §12 영향 목록에 plans/** · 06 §34 · §42 · Parameter 87→95 · VER · SEC · SCH-002~005 ·
   §24.2 · §21.3 · deploy/01_Schema.sql:132 가 전부 있다
4. 초안의 수량표가 상세정의와 자기정합이다 (실측으로 확인한다. 표를 믿지 마라)
5. V08 계수 방식 재설계안이 초안 자신에게 적용했을 때 실측 = 선언 이다
6. 초안을 grilling 으로 다시 압박했고 결과가 초안에 반영됐다
7. docs/baseline/** 이 한 바이트도 바뀌지 않았다        → verify-baseline.sh 6/6
8. node tools/verify-docs.js 가 PASS 15 / FAIL 0 이다
9. ./scripts/verify-winforms-unchanged.sh 가 exit 0 이다
10. git status --short 에 ROOT tools/ 와 docs/baseline/output/ 이 커밋되지 않았다
11. branch 가 phase4-r3 이고 phase4-database 는 fdfb8e6 그대로다
```

## 9. 검증되지 않은 것 — 마주치면 이렇게 하라

| 항목 | 상태 | 대응 |
|---|---|---|
| R3 스키마 | **한 번도 배포된 적 없다** | 초안 단계에서 배포하지 않는다. DDL 문법 확인이 필요하면 scratch 이름(`ZZ` 접두사)으로 만들고 반드시 지운 뒤 `tests/01_Schema_Tests.sql` 로 원상복구를 확인한다 |
| `d0.1` §2.5 *"한글 식별자 대괄호 없이 생성 · `EXCEPT` 에 `COLLATE` 불필요"* | 선행 세션 `database-38` 의 주장. **검토자 5명 중 아무도 재현하지 않았다**(전원 SQL Server 접속 금지) | 초안에 **미재현 표기**를 남긴다. 실측은 진행안 3단계다 |
| `Rebuild.sql`:29-32 cross-DB `NOT IN` 의 `Msg 468` | 정적 판정 불가 | 3단계에서 확인. 초안에는 위험으로만 기록 |
| `02` 의 시트 2·3·4·5·7 내용 | 시트 1·6 만 실제로 읽었다 | 개정 범위를 확정하기 전에 전부 읽는다 |
| Test ID 카탈로그 234건의 R3 재편성 | **미분석** | 초안 범위 밖. 기대값이 바뀌는 것만 §12.3 에 기록한다 |
| 계획서 SQL 의 **컬럼명** 게이트 | 없다 | `C4` 는 Table 축만 닫았다. 컬럼 축은 열려 있고 오탐 억제가 필요해 만들지 않았다 |

## 10. 끝나면

`d0.2` 경로와 §8 판정 11개를 표로 보고하고 **멈춘다.** 진행안 2단계(`02` 판독) 이후로 넘어갈지는 사용자가 정한다. 전체 진행안은 검토서 §9 에 있다.
