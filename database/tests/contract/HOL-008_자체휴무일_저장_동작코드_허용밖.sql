SET NOCOUNT ON;
-- [R22] 등록·수정을 하나로 합치면서 의도를 @휴무동작코드 로 받는다 (05 §12.5).
-- 허용 밖이면 101 이고 오류항목이 그 Parameter 이름이다. 실패 경로라 RS1 이 나가지 않는다.
-- [!] **이 갈래가 801 을 지킨다.** 코드 대신 「행이 있으면 수정」으로 유도했다면 [추가] 를
--     눌렀을 때 남의 행을 조용히 덮어쓴다 — 그래서 의도를 받고, 받은 값을 검증한다.
EXEC [dbo].[USP_HC_자체휴무일_저장] @휴무동작코드 = 'UPSERT', @휴무일자 = '2027-05-05', @휴무일명 = N'코드시험', @사용여부 = 1, @비고 = NULL;
GO
