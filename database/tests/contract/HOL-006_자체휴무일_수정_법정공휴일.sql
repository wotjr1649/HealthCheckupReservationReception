SET NOCOUNT ON;
-- 00 HOL-05: 법정·대체 공휴일은 화면이 바꾸지 않는다. **토큰이 맞아도** 802 다.
-- 휴무구분 확인이 행버전보다 앞이라 601 이 아니라 802 가 나온다 (05 §12.7).
DECLARE @Rv BINARY(8) = (SELECT [행버전] FROM [dbo].[휴무일] WHERE [휴무일자] = '2026-01-01');
EXEC [dbo].[USP_HC_자체휴무일_수정] '2026-01-01', @Rv, N'바꿔보기', 1, NULL;
GO
