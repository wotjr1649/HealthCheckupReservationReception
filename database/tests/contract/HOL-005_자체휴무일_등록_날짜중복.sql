SET NOCOUNT ON;
-- HOL-04 의 날짜 중복 금지. 이미 있는 Seed 날짜에 등록을 시도하면 801 이고 아무것도 바뀌지 않는다.
-- 실패 경로라 RS1 이 나가지 않는다 — 이 시험은 DB 를 바꾸지 않는다.
EXEC [dbo].[USP_HC_자체휴무일_저장] @휴무동작코드 = 'CREATE_HOLIDAY', @휴무일자 = '2026-12-26', @휴무일명 = N'중복시도', @사용여부 = 1, @비고 = NULL;
GO
