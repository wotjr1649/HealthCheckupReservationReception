SET NOCOUNT ON;
-- [X] plans/04 는 이 시나리오의 기대를 101 로 적었으나 실행으로 성립하지 않는다.
--     @주민번호 가 VARCHAR(13) 이라 14자리 값은 SP 진입 전에 조용히 절단된다(실측 확인).
--     SP 는 13자리만 보므로 길이 검증에 도달할 수 없다. 이것이 05 §2.2 가 C# 에 하이픈 포함
--     14자리 전달을 금지한 이유다. 관측 가능한 계약은 "절단된 13자리로 처리된다" 이므로
--     PWR-001 이 만든 행과 같은 주민번호·같은 이름이 되어 결과코드=2 가 나오는 것을 단언한다.
--     14자리 주민번호를 가진 행이 생기지 않는다는 것이 이 시험의 실질이다.
EXEC [dbo].[USP_HC_수검자_등록] 1, NULL, N'신규수검자', '90010110000188',
     NULL, NULL, NULL, NULL, NULL, NULL, NULL, 0, 0, N'TEST';
GO
