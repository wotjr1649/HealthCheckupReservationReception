# R17 — 차트번호·이름을 포함검색으로. 03 · 04 · 05 · 06.
import io, os, sys

ROOT = os.path.join(os.path.dirname(os.path.abspath(__file__)), '../..')


def edit(rel, pairs, label):
    p = os.path.join(ROOT, rel)
    s = io.open(p, 'r', encoding='utf-8', newline='').read()
    for old, new in pairs:
        if s.count(old) != 1:
            sys.exit('!! %s: [%s] %d 건' % (label, old[:60], s.count(old)))
        s = s.replace(old, new)
    io.open(p, 'w', encoding='utf-8', newline='').write(s)
    print('OK ' + label)


R17_03 = ("- **R17 개정 범위:** §5.3 의 검색방식 두 줄을 고친다 — **차트번호와 이름을 포함검색으로 연다**(`LIKE '%' + 값 + '%'`). 2026-09-10 사용자 결정이며, `F` 계열처럼 *일부만 아는 값*으로 찾는 것이 실제 접수 업무의 모양이라는 것이 이유다. 주민번호·생년월일·휴대전화는 **정확검색 그대로**다 — 신원값이라 부분일치가 뜻을 갖지 않는다. 결합은 여전히 `AND` 다(칸마다 따로 좁힌다). `04` §11.3 · `05` §7.2 가 같은 회차에서 열리고, `04` §11.3 의 「이름 `%검색어%` 포함검색 금지」가 이 회차로 사라진다. `[!]` **인덱스 seek 을 잃는다** — 앞에 `%` 가 붙으면 `UQ_수검자_CHART_NO` 도 `IX_수검자_NAME_BIRTHDAY` 도 못 탄다. 그 비용은 `06` §43-32 에 등재했다. 화면 ID·Navigation·Action·입력/ReadOnly 범위는 바뀌지 않는다\n")

R17_04 = ("- **R17 개정 범위:** §11.3 에서 **차트번호와 이름을 포함검색으로 연다**(`03` §5.3 재봉인에 딸린다). 같은 줄의 **「이름 `%검색어%` 포함검색은 허용하지 않는다」가 사라진다** — R16 이 조건 없는 전체조회를 이미 열어 그 금지가 막던 *가드 우회* 는 막을 것이 없어졌고, 남은 근거는 인덱스 seek 하나였다. 그 비용을 알고 여는 결정이다(`06` §43-32). §8.1.5 의 Index 목적 칸도 함께 고친다 — `UQ_수검자_CHART_NO` 와 `IX_수검자_NAME_BIRTHDAY` 가 더는 조회 seek 을 맡지 않는다. 메타문자 이스케이프는 그대로 남는다: 사용자가 친 `%` 는 여전히 글자 `%` 다. 테이블·컬럼·제약·Index **정의**는 한 줄도 바뀌지 않는다\n")

R17_05 = ("- **R17 개정 범위:** `03`·`04` 재봉인에 딸린다. §7.2 입력표의 검색방식 두 칸이다 — `@차트번호` 정확검색 → **포함검색**, `@성명` 접두검색 → **포함검색**. Parameter 이름·타입·NULL 허용·Result Set·정렬·결과코드는 한 줄도 바뀌지 않는다\n")

edit('docs/baseline/03_Wireframe_Definition.md', [
    ("- **문서 버전:** v1.9  (v1.5", "- **문서 버전:** v1.10  (v1.5"),
    ("v1.9 → R16 수검자 조회에 최소 조건이 없다)",
     "v1.9 → R16 수검자 조회에 최소 조건이 없다. v1.10 → R17 차트번호·이름 포함검색)"),
    ("- **기준선 ID:** `HC-RSV-RCP-20260910-R16`  (직전 `HC-RSV-RCP-20260909-R12`)\n",
     "- **기준선 ID:** `HC-RSV-RCP-20260910-R17`  (직전 `HC-RSV-RCP-20260910-R16`)\n" + R17_03),
    ("- 차트번호·주민번호·생년월일·휴대전화는 정규화 후 정확검색한다.\n- 이름은 앞부분 일치(`Name LIKE @Name + '%'`)로 검색한다.",
     "- **차트번호와 이름은 포함검색한다.** `[R17]` 친 값이 어디에 들어 있든 찾는다 — `F` 로 `F001`~`F020` 을 한 번에 본다.\n"
     "- 주민번호·생년월일·휴대전화는 정규화 후 **정확검색**한다. 신원값이라 부분일치가 뜻을 갖지 않는다.\n"
     "- 사용자가 친 `%`·`_`·`[` 는 **글자 그대로** 찾는다 (패턴 메타문자로 쓰지 않는다)."),
], '03 §5.3')

edit('docs/baseline/04_DB_Design.md', [
    ("- **문서 버전:** v3.6\n", "- **문서 버전:** v3.7\n"),
    ("- **기준선 ID:** `HC-RSV-RCP-20260910-R16`\n",
     "- **기준선 ID:** `HC-RSV-RCP-20260910-R17`\n" + R17_04),
    ("- 차트번호, 주민번호, 생년월일, 휴대전화 정규화값은 정확검색한다.\n- 성명은 접두검색(`성명 LIKE @성명 + '%'`)을 사용한다.",
     "- `[R17]` **차트번호와 성명은 포함검색**(`LIKE '%' + 값 + '%'`)한다. 주민번호·생년월일·휴대전화 정규화값은 **정확검색**한다."),
    ("- 이름 `%검색어%` 포함검색은 허용하지 않는다. `[R16]` 조건 없는 전체조회 금지는 수검자 목록에서 걷었다 — 위 개정 범위가 그 경위다.",
     "- `[R16]` 조건 없는 전체조회 금지를 걷었고, `[R17]` 이름 `%검색어%` 포함검색 금지도 걷었다 — 위 두 개정 범위가 그 경위다. 사용자가 친 메타문자를 글자로 다루는 이스케이프는 남는다."),
    ("| `UQ_수검자_CHART_NO` | `차트번호` | - | 차트번호 정확조회·고유성 |",
     "| `UQ_수검자_CHART_NO` | `차트번호` | - | 차트번호 고유성. `[R17]` 조회는 포함검색이라 이 Index 를 타지 않는다 |"),
    ("| `IX_수검자_NAME_BIRTHDAY` | `성명, 생년월일` | `수검자ID, 차트번호, 성별, 휴대전화` | 이름 조회·이름+생년월일 중복후보 |",
     "| `IX_수검자_NAME_BIRTHDAY` | `성명, 생년월일` | `수검자ID, 차트번호, 성별, 휴대전화` | 이름+생년월일 중복후보. `[R17]` 이름 조회는 포함검색이라 이 Index 를 타지 않는다 |"),
], '04 §11.3 · §8.1.5')

edit('docs/baseline/05_DB_Rule_SP_Contract.md', [
    ("- **문서 버전:** v3.7\n", "- **문서 버전:** v3.8\n"),
    ("- **기준선 ID:** `HC-RSV-RCP-20260910-R16`\n",
     "- **기준선 ID:** `HC-RSV-RCP-20260910-R17`\n" + R17_05),
    ("| `@차트번호` | `NVARCHAR(100)` | O | 정확검색 |\n| `@성명` | `NVARCHAR(100)` | O | 접두검색 |",
     "| `@차트번호` | `NVARCHAR(100)` | O | `[R17]` 포함검색 |\n| `@성명` | `NVARCHAR(100)` | O | `[R17]` 포함검색 |"),
], '05 §7.2')
