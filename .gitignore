# Build output
[Bb]in/
[Oo]bj/
.vs/
TestResults/
*.user

# 산출물은 추적한다 (2026-09-09 사용자 지시).
# 03 화면설계서는 배치가 있는 유일한 공개본이라 구현이 그것을 보고 만든다 —
# 커밋에 없으면 리뷰가 그 배치를 볼 수 없다.
# PowerPoint 잠금 파일만 뺀다.
docs/baseline/output/~$*
*.suo

# Claude Code 의 기계별 설정. 저장소 산출물이 아니다 (2026-09-09 사용자 결정).
# 추적 밖에 두면 git status 가 항상 더럽고, 그러면 "작업트리가 깨끗한가" 를 아무도 안 본다.
.claude/settings.local.json

# Phase 4 로그 (보고서는 커밋한다)
database/artifacts/logs/
integration/artifacts/logs/
winforms/artifacts/logs/
