# Phase 5 — 2026-09-12 (session 20). **알려진 결함 둘을 닫고, 잔여 표기를 바꿨다**

선행은 `2026-09-11-session-19-Phase5-Wrapup.md` 다. 그 문서의 §1 수치는 **그 시점의 실측**
이고 여기서 고치지 않는다 — 아래 §1 이 2026-09-12 의 실측이다.

---

## 1. 지금 참인 것 — 돌려서 본 것만 적는다

```text
winforms 게이트     PASS 115 · FAIL 0 · exit 0        ./scripts/test.sh
winforms 빌드       Debug 성공 (restore 0 · build 0)  MSBuild 16.11
winforms 시험       266 통과 · 실패 0                  vstest /Platform:x86
```

경고 하나가 남아 있고 이 세션의 것이 아니다 — `FrmHoliday.cs(77) CS0108`
(`FrmHoliday.Update()` 가 `Control.Update()` 를 가린다).

`database` 계열 회귀는 이 세션에서 돌리지 않았다. 손댄 것이 `winforms/` 와 점검용 데이타
뿐이라 DB 계약은 건드리지 않았다.

---

## 2. H1 — 마감 판정이 「시간대 전체」에서 「고른 시간대」로 좁았다

경위와 무엇이 틀렸었나는 `2026-09-11-Deadline-Field-Check.md` §4 가 갖는다. 여기 베끼지
않는다 (ROOT `AGENTS.md` §6). 결론만:

```text
고친 곳   ReservationPresenter.CutoffBlocked — 인자에 시간대코드가 붙었다
새 규칙   고른 시간대가 있으면 그것만 본다 · 미선택이면 AM·PM 이 모두 마감일 때만 전환
시험      특성화 시험을 지우고 옳은 동작 셋으로 바꿨다 (PM 선택 · 미선택 열림 · 미선택 둘 다 마감)
```

`[X]` **초판이 「설계가 필요하다」고 적은 이유가 틀린 전제였다.** `05` §9.6 을 「DB 가 시간대
하나를 골라 돌려준다」로 읽었는데, SP 는 **보낸 값을 그대로 돌려준다**
(`04_Procedures_Select.sql` 의 `[시간대코드] = CAST(@시간대코드 AS CHAR(2))`, 미선택이면
NULL). 전제를 실물에서 확인하자 판단할 것이 한 줄로 줄었다. 계약 문서만 읽고 멈췄으면
설계 회의가 한 번 더 필요했을 것이다.

---

## 3. R1 — 리본은 `[예약]` 인데 창 제목이 「신규 예약」이었다

`session-17` §4.9 가 `신규예약 → 예약` 으로 바꾼 것이 창 제목까지 닿지 않은 자리다.
`FrmReservation` 의 Designer `Text` 와 `Title` setter 기본값 둘 다 `예약` 이 되었다.

`[!]` **화면 ID 주석의 「신규 예약」은 그대로 둔다.** 그것은 `03` §2 가 정한 **화면명**이고
(`WF-RSV-01 | 신규 예약`) 창 제목과 다른 것이다. `SCR-001` 이 그 이름을 양방향으로 지킨다.

---

## 4. 시간대 줄의 잔여 — 「예약 후」에서 「예약 전」으로 (사용자 지시)

```text
전   오전  1 / 20 (예약 후 잔여 18)     ← 앞의 두 수와 하나 어긋나 보인다
후   오전  1 / 20 (예약 전 잔여 19)
```

`clsWorkText.FormatCapacityAfterBooking` 이 `FormatCapacityBeforeBooking(현재인원, 정원)`
으로 바뀌었다. **DB 의 `잔여자리` 를 더 이상 쓰지 않는다** — `SP-RSV-01` RS2 의 그 값은
`MAX(0, 정원 - 적용후인원)` 이고 신규는 `적용후인원 = 현재인원 + 1` 이라(05 §9.7) 앞의 두
수와 기준이 다르다. 같은 계약이 준 `정원`·`현재인원` 의 차이로 적으므로 규칙이 화면으로
새지 않고, `MAX(0, ...)` 의 바닥만 따라간다.

`03` 은 이 문구를 규정하지 않는다 — §8.3 에 잔여 표기가 없다. 확인하고 바꿨다.

---

## 5. 점검용 데이타 — 날짜를 미는 스크립트가 생겼다

`winforms/scripts/seed-today.sh`. 무엇을 왜 하는지는
`2026-09-11-Deadline-Field-Check.md` §5 가 갖는다.

`[!]` **날짜를 스크립트에 적지 않았다.** 가장 늦은 예약일이 오늘이 되도록 통째로 민다 —
멱등이고, 오늘이 업무일이 아니면 red 로 멈춘다. 그 판정은 `UFN_HC_일정확인` 이 한다.

2026-09-12 실행: `2026-09-11 → 2026-09-12`, 10건 이동, 유효업무 없는 수검자 55명 남음
(`00` RP-06 이 막히지 않는다).

---

## 6. 남은 것

`session-19` §4 의 여섯 줄은 그대로다. H1 은 그 표에 없었고 이제 닫혔다.

새로 연 것은 없다.
