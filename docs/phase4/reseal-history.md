# Phase 4 재봉인·소유권 이력

`database/AGENTS.md` 가 담고 있던 기록이다. **규칙이 아니라 기록**이므로 지침에서 내렸다 —
지침은 매 세션 적재되지만 이 기록은 필요할 때만 읽으면 된다. 옮긴 날 2026-09-08.

봉인·입주·재봉인 **규칙**은 여전히 ROOT `AGENTS.md` §2 이고, 여기는 이 계열이 실제로 밟은
발자국만 남긴다. 다음 재봉인은 이 선례를 따라간다.

## 1. 재봉인 이력

`[!]` R3 의 근거였던 초안 `04_DB_Design_R3_DRAFT.md` 는 2026-09-08 공개 정리로 **삭제했다**(세션 프롬프트 8회분도 같이).
이력에는 그대로 있다 — 필요하면 이렇게 꺼낸다.

```bash
git log --all --oneline -- docs/phase4/04_DB_Design_R3_DRAFT.md
git show <커밋>:docs/phase4/04_DB_Design_R3_DRAFT.md
```

```text
R17 2026-09-10. 03·04·05·06 넷. **차트번호와 이름을 포함검색으로 연다.**
      사용자가 실행본을 눌러 보고 지적했다 — 일부만 아는 값으로도 찾아야 한다.
      03 v1.10  §5.3 검색방식. 주민번호·생년월일·휴대전화는 정확검색 그대로
      04 v3.7   §11.3 + §8.1.5 Index 목적 칸.
                「이름 %검색어% 포함검색 금지」가 이 회차로 사라진다
      05 v3.8   §7.2 입력표 두 칸. SELECT_예약접수목록(§8.1)은 정확검색 그대로
      06 v1.14  §4.1 회차표 + §43-32 한계 등재 (Index seek 상실)
      SP        @차트번호패턴·@성명패턴 을 '%값%' 으로. 이스케이프는 남긴다
      시험      02b_수검자목록_차트번호_포함검색 — '01' 은 어느 차트번호의 접두도
                아니므로 정확·접두 어느 쪽이어도 0건이다. 포함검색만 행을 낸다
      [I] R16 이 전체조회를 열면서 이름 포함검색 금지가 지키던 것이 인덱스 seek 하나만
          남았다. R17 은 그 비용을 알고 지불한다 — 06 §43-32 에 적었다.
      함께 (봉인 밖) 중복 클릭 방지 — 사용자 요청
        Views/clsBusyScope.cs   버튼 잠금 + 대기 커서. DoEvents 를 쓰지 않는다
        재진입 가드             [조회]는 Ribbon 과 화면 버튼 둘이 같은 Action 을 부르므로
                                버튼 잠금만으로는 구멍이 남는다

R16 2026-09-10. 03·04·05·06 넷. **수검자 목록의 「최소 1개 조건」을 걷는다.**
      사용자가 실행본을 직접 눌러 보고 지적했다 — 창을 열면 Grid 가 비어 있어
      무엇을 검색해야 하는지 알 수 없다. 조건이 없으면 전체를 조회하고,
      화면은 열릴 때 그 경로로 한 번 채운다.
      03 v1.9   §5.3 최소 조건 제거 (§7.2 가 §5.3 에 위임하므로 DLG-PAT-02 도 따라온다)
      04 v3.6   §11.3 — 최소 조건과 '조건 없는 전체조회 금지' 를 걷는다.
                이름 %검색어% 포함검색 금지는 **남긴다** (별개 규칙)
      05 v3.7   §7.2 의 103 → 전체 조회 · §13 허용코드에서 103 제거
      06 v1.13  §4.1 회차표 + §43-31 한계 등재
      SP        deploy/04_Procedures_Select.sql 의 103 가드 제거
      시험      02_수검자목록_조건없음 → 02_수검자목록_전체조회 (기대 0 + RS1)
                allowed-codes 에서 103 제거 · Presenter 시험 셋 정정
      화면      가드 제거 + UcPatientManagement.OnLoad 가 LoadInitial 을 부른다
      [X] **동결(verify-db-frozen.sh)을 연 첫 회차다.** 게이트가 red 를 내며 회차 이름을
          요구했고 그대로 따랐다 — 동결은 '고치지 마라' 가 아니라 '고쳤으면 회차를 붙여라'.
      [X] 초판이 OnLoad 에서 SearchRequested 를 올렸다. 그 경로는 실패를 모달로 알리는데
          fake 의 결과가 null 이라 UI 시험이 예외 대화상자에 걸려 멈췄다(실측).
          LoadInitial 은 조용히 실패한다 — 사용자가 부탁하지 않은 호출이기 때문이다.
      [!] 주민번호 노출을 사용자에게 알리고 승인받았다 (06 §43-31). 과제는 임의 시험값만
          쓰고(00 §2.1) Grid 가 기본 숨김이지만, 운영 전 반드시 다시 봐야 한다.

R15 2026-09-10. 06 만. **남은 위험을 닫는 회차.** 계약·SQL·Seed·시험은 한 줄도 안 바꿨다.
      사용자가 /grilling 으로 넷을 결정했다 (2026-09-10).
      ① §42 창 안 회차   논거로 닫는다 — R13 이 CON 과 성공 경로를 창 밖에서 다 쟀고,
                          남는 한 가지("진짜 창 안에서 창이 열린다")는 RUL-T01~T04 와
                          OFF-309-02 가 매 회차 진짜 값으로 잰다. 네 겹이 겹친다
      ② §43-22          700/701 순서 비대칭을 Phase 5 범위에서 닫는다 — 07 §6.1 이 둘을
                          한 칸으로 표현하므로 화면이 가르지 않는다. 05 를 열면 DB 변경이고
                          얻는 것이 없다
      ③ §42 Gate 표      G17 신설 등재 (라이브 DB ↔ deploy/)
      04·05 는 열지 않았다 — 수치도 계약도 안 바뀌었다.

      함께 만든 것 (봉인 밖이라 회차에 안 들어간다)
        scripts/verify-live-sync.sh   G17. SYNC-001~003 + 자체시험 3건.
                                      실측: 라이브 24개 모듈 == deploy/ 바이트 일치.
                                      SYNC-004 는 05 §1.1 이 서버·인스턴스를 선언하지 않아
                                      화면이 다른 DB 를 볼 수 있던 자리를 닫는다
        verify-docs.js V26            정책 시각값 29건 ↔ 00 CP-04·§3.
                                      음성시험: 00 을 18:00->19:00 으로 바꾸면 15곳을 지목한다
        07 §14                        미해결 이탈 5건을 0건으로. X-05·X-04·X-09 는 이미
                                      고쳐졌는데 표만 안 바뀌어 있었다
        킷 references/mvp-wiring.md   X-07 — 예제의 + ex.Message 를 걷었다 (킷 §6 위반)

R14 2026-09-10. 04·05·06 셋. **R13 의 잔재 — 문서가 자기 목록과 다른 수를 말하고 있었다.**
      R13 이 7번째 테이블을 더했는데 세 문서 본문 열여섯 곳이 R13 이전의 수를 그대로 말했다.
      그중 04 §0.4.1 은 "후속 Phase에서 테이블을 추가하지 않는다" 는 **동결 선언**이었다 —
      R13 이 실제로 그것을 깨고도 그 줄을 열지 않은 셈이다.
      더 파 보니 05 머리말의 「확정 객체」가 말하는 외부 호출 SP 수가 R7 이후 §1.3 과
      어긋나 있었고, 같은 값이 공개본 시트 제목에 하드코딩돼 **R7 부터 거짓을 내보내고
      있었다**(생성기 넷 중 셋이 그랬다: 테이블 수 · SP 수 · ERD Entity/Table).
      04 v3.5  현재를 주장하는 문장에서 수를 걷는다. 역사 기록은 그대로 둔다   + 해시 1개
      05 v3.6  §0.1·§0.2 + 머리말 확정 객체                                  + 해시 1개
      06 v1.11 §13 예외 문장 + §16 의 04 인용 + §4.1 회차표                   + 해시 1개
      생성기  build_04·build_05·erd_04 의 시트 제목을 **세서 넣게** 바꿨다 (하드코딩 제거)
      게이트  verify-docs.js V25 — 봉인 문서의 개수 주장 ↔ 04 §7 · 05 §1.3. 음성시험 확인
      SQL·Seed·시험·계약은 한 줄도 바뀌지 않았다 — 회차 R13 의 증거가 그대로 유효하고
      06 §42 에 회차를 더하지 않는다 (R10·R11 과 같은 성격이다).

R13 2026-09-10. 04·05·06 셋. **운영시간·마감시각을 SP 리터럴에서 기준정보 테이블로 옮긴다.**
      뿌리는 06 §43-1 과 §43-14 다 — 여섯 값이 UFN_HC_일정확인·SP-COM-01 본문에 박혀 있어
      시험이 창을 만들 수 없었고, 그래서 예약·접수 성공 경로가 하루 아홉 시간 안에서만
      판정됐다. 06 이 남긴 답은 "사람이 OS 시계를 옮긴다" 뿐이었다.
      04 §8.7 테이블 운영기준 (7컬럼 · PK · SINGLETON · HOURS)                + 해시 1개
      05 §2.4 · §6.1.4 · §7.1 — 값이 아니라 **어디서 읽는지**를 적는다        + 해시 1개
      06 §14a 신설 + §33.2a 업무일 분기 + §42 회차 R13 + §43 의 1·12·14·21    + 해시 1개
      00·01·02·03 은 열지 않았다 — 수치의 소유자는 00 이고 그 값이 **안 바뀌었다**.
        정책을 옮긴 것이 아니라 SP 가 그 값을 읽는 경로를 바꾼 것이다.
      [!] 가드레일 셋이 이 회차의 조건이었다 (사용자 승인 2026-09-09).
          ① 관리 CRUD SP 를 만들지 않는다 — 00 §8.2 밖을 유지하므로 "설정" 이 아니다
          ② 게이트가 Seed 값 == 00 을 판정한다        verify-operating-baseline.sh OPR-G3
          ③ test.sh 가 회차 끝에 값이 되돌아왔는지 판정한다                    OPR-G4
          셋째가 없으면 남은 창이 이후 회차를 다른 제품으로 만든다 (06 §43-14 가 배포 원본
          수정을 금지한 그 이유다 — 다른 점은 이쪽엔 그것을 보는 눈이 있다는 것뿐이다)
      실측: 창 밖 회차가 R12 의 PASS 273 · SKIP 56 · NOT RUN 9 에서
        PASS 403 · SKIP 0 · NOT RUN 1 이 됐다 (2026-09-10 00시대 · 운영시작 9시간 전).
        창 안 최고 회차(R9 창 안 399·2·1)보다도 높다. OFF-308 과 OFF-309 가 한 회차 안에서
        함께 판정된 것, CON-001~008 이 창 밖에서 판정된 것 둘 다 처음이다.

R12 2026-09-09. 00·03·05·06 넷. **공통 업무 가능조건의 적용 대상을 예약·접수로 한정한다.**
      뿌리는 06 §43-23 이 이미 "계약의 비대칭" 으로 등재해 둔 것이다 — 05 §12 의 휴무일 SP 넷은
      같은 기준정보인데 처음부터 308/309 를 적용하지 않았고 01 도 P01(수검자)에 업무일 분기를
      두지 않았는데, 05 §10.1·§10.2 와 06 §21.2 만 수검자를 검진 업무로 묶고 있었다.
      00 §1.1 적용 대상 + UI 차단 금지                                     + 해시 1개
      03 §1.3 · §5.2 · §9.6 · §9.7 의 「공통 업무불가」 행 · §24 예외 서술   + 해시 1개
      05 §10.1 · §10.2 검증순서 · §13 두 행                                 + 해시 1개
      06 §4.1 회차표 + §15.2 fixture 이유 + §21.2 두 행 + §33.2a + §42 회차 + §43 의 12·23  + 해시 1개
      04·01·02 는 열지 않았다 — 04 §451 은 운영시간의 정의만 적고, 01 은 P01 에 업무일 분기가
        없으며, 02 는 업무시간·308·309 언급이 0건이다(xlsx 문자열 실측)
      [X] 05 만 고치고 06 을 안 열면 계약이 두 곳에서 다른 말을 한다 — 작업 중 한 번 밟았다가
          사용자 지적으로 되잡았다. 06 §21.2 이 검증순서와 허용 ResultCode 를 따로 갖고 있다
      하드코딩 생성기도 함께 열었다 (V24): xlsx/build_00.js · wireframe/z_d_ribbon.js ·
        screens/wf_00.js · screens/a0_list.js · screens/wf_pat_01.js · screens/wf_wrk_01.js
      공개본을 다시 만들었다. docProps 를 뺀 파트 비교로 실제 바뀐 것은 00(5파트)·03(4슬라이드)뿐
      SQL·시험도 같은 회차에서 바꿨다 — deploy/05_Procedures_Patient_Write.sql 의 게이트 3곳,
        tests/05·08 의 창 밖 분기, contract/OFF-308-01 을 예약등록으로 옮겨 308 커버리지를 지킨다
      [X] OFF-309-01 은 만들었다 지웠다. "업무시간 밖 수검자등록이 309 가 아니다" 를 재려 했는데,
          R12 로 PWR-002 가 가드 없이 돌게 되어 창 밖에서 같은 것을 이미 잰다 — 순증 0 이다.
          이름은 309 단언을 암시하는데 내용은 결과코드 2 라 읽는 사람을 속이기까지 한다. 리뷰가 잡았다
      회귀 회차 R12 창 밖: exit 0 · PASS 274 · FAIL 0 · SKIP 56 · NOT RUN 9
        같은 창 밖인 R9 대비 PASS +47 · SKIP -22. PWR 계열이 창 밖에서 처음 판정됐다

R3  R3 초안 §3 진행안 4·5·6단계로 00~05 전체를 R2 -> R3
R4  plans/11-korean-sp-rename.md 의 승인된 매핑표로 04·05 만
      04 §3.3 명명규칙 · §0.2 개정기록 · 머리말                + 해시 1개
      05 §1.2 §1.6 §3.1 §6~§13 §16 · 머리말                     + 해시 1개
      00·01·02·03 은 열지 않았다 — SP 이름·Parameter·Result Set 컬럼이 그 넷에 0건이다(실측)
      치환은 손으로 하지 않았다. 단일 출처는 tools/r4-rename-map.json 이고
      node tools/r4-rename.js check 가 매핑의 빠짐·충돌을 먼저 판정한다
06 입주  2026-09-08. FINAL/GO 이므로 ROOT AGENTS.md §2.1 대로 docs/baseline/ 으로 들였다 + 해시 1개
R5  2026-09-08. Phase 4 종료 점검에서 §42 Gate 세 칸이 실측과 어긋난 것을 찾아 고쳤다
      06 머리말 · §4.1 · §42 회차표(회차 C) · G00 · G13 · G16          + 해시 1개
      04 머리말 · §0.1 상호참조                                          + 해시 1개
      05 는 열지 않았다 — 고칠 문장이 0건이다(실측)
      G00  06 입주로 봉인이 6 -> 7 이 됐는데 "6/6 일치" 로 남아 있었다. test.sh 도 같은 값을
           하드코딩해 매 회차 거짓을 찍었다 — 게이트 출력을 그대로 옮기게 고쳤다
      G13  (c) 가 `REVIEWED` 인데 06 자신이 요구한 artifacts/reports/allowlist-review.md 가
           없었다. tools/allowlist-review.js 를 만들어 회귀 안에서 매 회차 재생성한다
      G16  근거가 "본 문서 v1.0" 인데 그때 이미 v1.1 이었다 — 버전 대신 §4.1 을 가리키게
      04 를 연 이유는 §0.1 이 "04~06 이 R4" 를 **베껴** 적어 06 의 R5 재봉인에 거짓이
           되었기 때문이다. 값을 고치지 않고 06 §4.1 을 가리키게 바꿔 원인을 없앴다(§6)
R6  2026-09-08. 06 만. R5 가 §42 에 "회차 C 이후 저장소가 한 바이트도 안 바뀌었다" 는
      **미래 단정**을 남겼는데 다음 커밋 하나에 거짓이 됐다. 봉인 문서는 자기 밖 상태를
      주장할 수 없다 — 그 줄을 걷었다.  06 머리말 · §4.1 · §42        + 해시 1개
      04 는 열지 않았다 — R5 에서 06 §4.1 을 가리키게 바꿔 둔 덕분에 연쇄가 끊겼다(실측)
```

`06` 이 봉인 안으로 들어온 결과가 지금 `database/AGENTS.md` §2 에 규칙으로 남아 있다 —
DB 계약이 바뀌면 `04`·`05`·`06` 세 파일과 해시를 같은 커밋에 묶는다.

## 2. ROOT `tools/` · `docs/baseline/output/` 인수 경위

2026-09-07 사용자 승인으로 이 계열이 인수했다. 원래는 세션 `a97cfb9f` 몫이었고
R3 초안 `:1903` 이 그 순서를 정해 뒀는데(위 §1 의 명령으로 이력에서 꺼낸다), 그 세션이
존재하지 않아 R3 재봉인 뒤 산출물이 R2 에 머물러 있었다. 그래서 여기서 고치고 돌린다.

산출물 규칙 자체는 ROOT `AGENTS.md` §3 이고, 이 계열의 쓰기 경계는
`database/AGENTS.md` §3 이다.
